# Multiply Mask Source

Source Type ID: `dynamic_asset_generator:mask/multiply`

Format:

```json
{
    "type": "dynamic_asset_generator:mask/multiply",
    "sources": [
        {   },
        {   }
    ]
}
```

Multiplies the alpha channels of the provided sources, scaling to the range [0, 255].